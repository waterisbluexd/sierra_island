export component Island inherits Window {
    width: 200px;
    height: 60px;

    Rectangle {
        width: parent.width;
        height: parent.height;

        background: black;
        border-radius: 30px;
    }
}
